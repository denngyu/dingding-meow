# 宣传页监控演示图

## 现状

- 首屏监控预览使用纯色假画面和 CSS 检测框，用户无法直观看出人脸与水杯识别效果。
- `assets/preview_camera.png` 已有脱敏的演示帧，可作为页面静态示例。

## 目标与验收

- 监控气泡显示 `assets/preview_camera.png`，不再用 CSS 假截图。
- 图片保持 4:3，不拉伸，检测框与标签清晰可见。
- 标题明确为低频预览并提示省 CPU，不制造实时视频或卡顿的误解。
- 页面相关自动化测试通过，并完成浏览器截图检查。

## 状态

已完成。

## 实现

- 首屏监控气泡嵌入 `assets/preview_camera.png`。
- 删除 CSS 伪造的人脸框与杯子框，直接展示演示帧中的检测结果。
- 监控标题由 `LIVE / CAM 0` 改为 `低频预览 / 省 CPU`。
- 图片使用固定 4:3 容器与 `object-fit: cover`，并补充可访问性替代文本。

## 验证

- 新增测试 `test_promo_camera_preview_uses_a_real_demo_frame`，先失败后通过。
- Edge 无头浏览器完成桌面 1440×1000 与窄屏 390×844 截图检查，演示图无拉伸、无溢出。
- 完整测试共 65 项，本次相关测试通过；原有 `away.png` 尺寸为 341×384，导致旧的 384×384 规范测试产生 1 个失败与 1 个越界错误，本次未改动该素材。

## 风格统一版替换

- 使用项目猫咪的柔和插画质感与米白、炭黑、赤陶橙配色，生成虚构办公人物侧脸举杯演示帧。
- 演示帧明确区分侧脸与倾斜水杯检测框，眼镜没有被单独框选。
- 新图尺寸为 1448×1086，比例为 4:3，已覆盖 `assets/preview_camera.png`。
- 页面引用测试通过；Edge 首屏截图检查通过，记录为 `2026-07-15_宣传页监控演示图_v2.png`。

## 监控气泡完整展示

- 根因是演示台使用底部对齐叠放监控气泡、状态气泡和猫咪，总高度超过原来的 1:1.15 容器，顶部被 `overflow:hidden` 裁切。
- 演示台纵横比调整为 1:1.32，保留原有边界裁剪和取景框结构，不让内容溢到首屏背景。
- 新增 `test_promo_stage_is_tall_enough_to_show_the_full_monitor_bubble`，测试从 1.15 不足失败，在 1.32 下通过。
- Edge 桌面与 390px 窄屏截图确认监控框顶部、标题、图像与尾尖均完整显示。
