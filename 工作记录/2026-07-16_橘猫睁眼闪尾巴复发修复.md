# 2026-07-16 橘猫睁眼闪尾巴复发修复

## 目标

- 修复暖橘皮肤展开监控画面时，偶发闪出带尾巴坐姿的问题。
- 不修改橘猫画风和日常姿态，只约束错误的跨状态取图。

## 改动

- `cat_sprites.py`
  - 保留睁眼状态优先选择 `watch` 的现有规则。
  - 新增最终显示不变量：睁眼时若上游意外返回 `idle` 或 `tail`，强制收敛为 `watch`。
- `pet.py`
  - Canvas 绘制前调用最终显示不变量，避免未来状态或动画改动重新混入带尾巴姿态。
- `tests/test_cat_sprites.py`
  - 增加最终绘制保护测试。
- `tests/test_skin_system.py`
  - 使用暖橘皮肤实际图片验证睁眼与眨眼周期始终取 `watch.png`。
- `工具/preview_orange_open_eye.py`
  - 无摄像头、无托盘、无业务日志地运行暖橘睁眼预览 12 秒，覆盖多个眨眼周期。

## 验收标准

- 暖橘皮肤在睁眼和眨眼时都只显示 `watch.png`。
- 端杯等明确业务姿态不被最终保护误改。
- 语法检查、针对性测试、全量测试和启动级预览全部通过。

## 验收结果

- 语法检查：通过。
- 猫姿态与皮肤针对性测试：18 / 18 通过。
- 全量自动化测试：118 / 118 通过。
- 启动级预览：`工具/preview_orange_open_eye.py` 正常运行 12 秒并自动退出，覆盖两个以上眨眼周期。
